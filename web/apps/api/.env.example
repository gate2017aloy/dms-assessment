# SQLite in-memory database with shared cache so Prisma and seed share the same DB
DATABASE_URL="file::memory:?cache=shared"

# JWT secret — must match the frontend Next.js app for cookie-based auth
JWT_SECRET=""

# Frontend URL for CORS
FRONTEND_URL="http://localhost:3000"

# Server port
PORT=4000
